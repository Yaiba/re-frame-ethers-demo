(ns re-frame-ethers-demo.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [re-frame-ethers-demo.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
